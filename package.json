{
  "name": "august-herbarium-card",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check src/main.js && node --check src/herbarium/config.js && node --check src/herbarium/scenes.js && node --check src/herbarium/photo-engine.js && node --check src/herbarium/arrangement-engine.js && node --check src/herbarium/postcard-renderer.js && node --check scripts/dev-server.mjs && node --check scripts/validate.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm run validate && npm test"
  }
}
